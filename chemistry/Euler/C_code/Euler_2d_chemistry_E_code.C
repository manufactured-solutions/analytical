#include <math.h>

double SourceQ_E (
  double x,
  double y,
  double p_0,
  double p_x,
  double p_y,
  double rho_N_0,
  double rho_N_x,
  double rho_N_y,
  double rho_N2_0,
  double rho_N2_x,
  double rho_N2_y,
  double u_0,
  double u_x,
  double u_y,
  double v_0,
  double v_x,
  double v_y,
  double a_px,
  double a_py,
  double a_rho_N_x,
  double a_rho_N_y,
  double a_rho_N2_x,
  double a_rho_N2_y,
  double a_ux,
  double a_uy,
  double a_vx,
  double a_vy,
  double L,
  double M_N,
  double M_N2,
  double k_b1N,
  double k_f1N,
  double k_b1N2,
  double k_f1N2,
  double h0_N,
  double h0_N2)
{
  double Q_E;
  Q_E = -(p_0 + p_x * cos(a_px * PI * x / L) + p_y * sin(a_py * PI * y / L)) * (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * (u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L)) * sin(a_rho_N2_x * PI * x / L) * a_rho_N2_x * PI * rho_N2_x * M_N * M_N2 / L * pow((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2, -0.2e1) + (p_0 + p_x * cos(a_px * PI * x / L) + p_y * sin(a_py * PI * y / L)) * (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * (v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L)) * cos(a_rho_N2_y * PI * y / L) * a_rho_N2_y * PI * rho_N2_y * M_N * M_N2 / L * pow((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2, -0.2e1) + (p_0 + p_x * cos(a_px * PI * x / L) + p_y * sin(a_py * PI * y / L)) * (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * (u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L)) * cos(a_rho_N_x * PI * x / L) * a_rho_N_x * PI * rho_N_x * M_N2 * M_N2 / L * pow((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2, -0.2e1) - (p_0 + p_x * cos(a_px * PI * x / L) + p_y * sin(a_py * PI * y / L)) * (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * (v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L)) * sin(a_rho_N_y * PI * y / L) * a_rho_N_y * PI * rho_N_y * M_N2 * M_N2 / L * pow((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2, -0.2e1) - (p_0 + p_x * cos(a_px * PI * x / L) + p_y * sin(a_py * PI * y / L)) * (u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L)) * cos(a_rho_N_x * PI * x / L) * a_rho_N_x * PI * rho_N_x * M_N2 / L / ((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2) + (p_0 + p_x * cos(a_px * PI * x / L) + p_y * sin(a_py * PI * y / L)) * (v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L)) * sin(a_rho_N_y * PI * y / L) * a_rho_N_y * PI * rho_N_y * M_N2 / L / ((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2) - (p_0 + p_x * cos(a_px * PI * x / L) + p_y * sin(a_py * PI * y / L)) * (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * cos(a_ux * PI * x / L) * a_ux * PI * M_N2 * u_x / L / ((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2) - (p_0 + p_x * cos(a_px * PI * x / L) + p_y * sin(a_py * PI * y / L)) * (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * cos(a_vy * PI * y / L) * a_vy * PI * M_N2 * v_y / L / ((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2) - ((0.7e1 * rho_N2_0 + 0.7e1 * rho_N2_x * cos(a_rho_N2_x * PI * x / L) + 0.7e1 * rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (0.5e1 * rho_N_0 + 0.5e1 * rho_N_x * sin(a_rho_N_x * PI * x / L) + 0.5e1 * rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2) * (u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L)) * sin(a_px * PI * x / L) * a_px * PI * p_x / L / ((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2) / 0.2e1 + ((0.7e1 * rho_N2_0 + 0.7e1 * rho_N2_x * cos(a_rho_N2_x * PI * x / L) + 0.7e1 * rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (0.5e1 * rho_N_0 + 0.5e1 * rho_N_x * sin(a_rho_N_x * PI * x / L) + 0.5e1 * rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2) * (v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L)) * cos(a_py * PI * y / L) * a_py * PI * p_y / L / ((rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * M_N + (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) * M_N2) / 0.2e1 - (pow(u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L), 0.2e1) + pow(v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L), 0.2e1) + 0.2e1 * h0_N2) * (u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L)) * sin(a_rho_N2_x * PI * x / L) * a_rho_N2_x * PI * rho_N2_x / L / 0.2e1 + (pow(u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L), 0.2e1) + pow(v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L), 0.2e1) + 0.2e1 * h0_N2) * (v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L)) * cos(a_rho_N2_y * PI * y / L) * a_rho_N2_y * PI * rho_N2_y / L / 0.2e1 + (pow(u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L), 0.2e1) + pow(v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L), 0.2e1) + 0.2e1 * h0_N) * (u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L)) * cos(a_rho_N_x * PI * x / L) * a_rho_N_x * PI * rho_N_x / L / 0.2e1 - (pow(u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L), 0.2e1) + pow(v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L), 0.2e1) + 0.2e1 * h0_N) * (v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L)) * sin(a_rho_N_y * PI * y / L) * a_rho_N_y * PI * rho_N_y / L / 0.2e1 + ((0.3e1 * rho_N_0 + 0.3e1 * rho_N2_0 + 0.3e1 * rho_N_x * sin(a_rho_N_x * PI * x / L) + 0.3e1 * rho_N2_x * cos(a_rho_N2_x * PI * x / L) + 0.3e1 * rho_N_y * cos(a_rho_N_y * PI * y / L) + 0.3e1 * rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * pow(u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L), 0.2e1) + (rho_N_0 + rho_N2_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * pow(v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L), 0.2e1) + 0.2e1 * h0_N * (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) + 0.2e1 * h0_N2 * (rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) + 0.7e1 * p_0 + 0.7e1 * p_x * cos(a_px * PI * x / L) + 0.7e1 * p_y * sin(a_py * PI * y / L)) * cos(a_ux * PI * x / L) * a_ux * PI * u_x / L / 0.2e1 + ((rho_N_0 + rho_N2_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * pow(u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L), 0.2e1) + (0.3e1 * rho_N_0 + 0.3e1 * rho_N2_0 + 0.3e1 * rho_N_x * sin(a_rho_N_x * PI * x / L) + 0.3e1 * rho_N2_x * cos(a_rho_N2_x * PI * x / L) + 0.3e1 * rho_N_y * cos(a_rho_N_y * PI * y / L) + 0.3e1 * rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * pow(v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L), 0.2e1) + 0.2e1 * h0_N * (rho_N_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L)) + 0.2e1 * h0_N2 * (rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) + 0.7e1 * p_0 + 0.7e1 * p_x * cos(a_px * PI * x / L) + 0.7e1 * p_y * sin(a_py * PI * y / L)) * cos(a_vy * PI * y / L) * a_vy * PI * v_y / L / 0.2e1 - (sin(a_uy * PI * y / L) * a_uy * u_y + sin(a_vx * PI * x / L) * a_vx * v_x) * (rho_N_0 + rho_N2_0 + rho_N_x * sin(a_rho_N_x * PI * x / L) + rho_N2_x * cos(a_rho_N2_x * PI * x / L) + rho_N_y * cos(a_rho_N_y * PI * y / L) + rho_N2_y * sin(a_rho_N2_y * PI * y / L)) * (u_0 + u_x * sin(a_ux * PI * x / L) + u_y * cos(a_uy * PI * y / L)) * (v_0 + v_x * cos(a_vx * PI * x / L) + v_y * sin(a_vy * PI * y / L)) * PI / L;
  return(Q_E);
}
