rho_an_N = rho_N_0 + rho_N_x * sin(a_rho_N_x * pi * x / L);
rho_an_N2 = rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * pi * x / L);
rho_an = rho_N_0 + rho_N_x * sin(a_rho_N_x * pi * x / L) + rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * pi * x / L);
p_an = p_0 + p_x * cos(a_px * pi * x / L);
u_an = u_0 + u_x * sin(a_ux * pi * x / L);
grad_rho_an_N[0] = rho_N_x * cos(a_rho_N_x * pi * x / L) * a_rho_N_x * pi / L;
grad_rho_an_N[1] = 0;
grad_rho_an_N[2] = 0;
grad_rho_an_N2[0] = -rho_N2_x * sin(a_rho_N2_x * pi * x / L) * a_rho_N2_x * pi / L;
grad_rho_an_N2[1] = 0;
grad_rho_an_N2[2] = 0;
grad_rho_an[0] = rho_N_x * cos(a_rho_N_x * pi * x / L) * a_rho_N_x * pi / L - rho_N2_x * sin(a_rho_N2_x * pi * x / L) * a_rho_N2_x * pi / L;
grad_rho_an[1] = 0;
grad_rho_an[2] = 0;
grad_p_an[0] = -p_x * sin(a_px * pi * x / L) * a_px * pi / L;
grad_p_an[1] = 0;
grad_p_an[2] = 0;
grad_u_an[0] = u_x * cos(a_ux * pi * x / L) * a_ux * pi / L;
grad_u_an[1] = 0;
grad_u_an[2] = 0;
