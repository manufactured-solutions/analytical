rho_an_N = rho_N_0 + rho_N_x * sin(a_rho_N_x * pi * x / L) + rho_N_t * cos(a_rho_N_t * pi * t / L);
rho_an_N2 = rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * pi * x / L) + rho_N2_t * sin(a_rho_N2_t * pi * t / L);
rho_an = rho_N_0 + rho_N_x * sin(a_rho_N_x * pi * x / L) + rho_N_t * cos(a_rho_N_t * pi * t / L) + rho_N2_0 + rho_N2_x * cos(a_rho_N2_x * pi * x / L) + rho_N2_t * sin(a_rho_N2_t * pi * t / L);
u_an = u_0 + u_x * sin(a_ux * pi * x / L) + u_t * cos(a_ut * pi * t / L);
T_an = T_0 + T_x * cos(a_Tx * pi * x / L) + T_t * cos(a_Tt * pi * t / L);
Tv_an = Tv_0 + Tv_x * cos(a_Tvx * pi * x / L) + Tv_t * sin(a_Tvt * pi * t / L);
grad_rho_an_N[0] = rho_N_x * cos(a_rho_N_x * pi * x / L) * a_rho_N_x * pi / L;
grad_rho_an_N[1] = 0;
grad_rho_an_N[2] = 0;
grad_rho_an_N2[0] = -rho_N2_x * sin(a_rho_N2_x * pi * x / L) * a_rho_N2_x * pi / L;
grad_rho_an_N2[1] = 0;
grad_rho_an_N2[2] = 0;
grad_rho_an[0] = rho_N_x * cos(a_rho_N_x * pi * x / L) * a_rho_N_x * pi / L - rho_N2_x * sin(a_rho_N2_x * pi * x / L) * a_rho_N2_x * pi / L;
grad_rho_an[1] = 0;
grad_rho_an[2] = 0;
grad_u_an[0] = u_x * cos(a_ux * pi * x / L) * a_ux * pi / L;
grad_u_an[1] = 0;
grad_u_an[2] = 0;
grad_T_an[0] = -T_x * sin(a_Tx * pi * x / L) * a_Tx * pi / L;
grad_T_an[1] = 0;
grad_T_an[2] = 0;
grad_Tv_an[0] = -Tv_x * sin(a_Tvx * pi * x / L) * a_Tvx * pi / L;
grad_Tv_an[1] = 0;
grad_Tv_an[2] = 0;
