rho_an_C = rho_C_0 + rho_C_x * sin(a_rho_C_x * pi * x / L);
rho_an_C3 = rho_C3_0 + rho_C3_x * cos(a_rho_C3_x * pi * x / L);
rho_an = rho_C_0 + rho_C_x * sin(a_rho_C_x * pi * x / L) + rho_C3_0 + rho_C3_x * cos(a_rho_C3_x * pi * x / L);
u_an = u_0 + u_x * sin(a_ux * pi * x / L);
T_an = T_0 + T_x * cos(a_Tx * pi * x / L);
grad_rho_C_an[0] = rho_C_x * cos(a_rho_C_x * pi * x / L) * a_rho_C_x * pi / L;
grad_rho_C_an[1] = 0;
grad_rho_C_an[2] = 0;
grad_rho_C3_an[0] = -rho_C3_x * sin(a_rho_C3_x * pi * x / L) * a_rho_C3_x * pi / L;
grad_rho_C3_an[1] = 0;
grad_rho_C3_an[2] = 0;
grad_rho_an[0] = rho_C_x * cos(a_rho_C_x * pi * x / L) * a_rho_C_x * pi / L - rho_C3_x * sin(a_rho_C3_x * pi * x / L) * a_rho_C3_x * pi / L;
grad_rho_an[1] = 0;
grad_rho_an[2] = 0;
grad_u_an[0] = u_x * cos(a_ux * pi * x / L) * a_ux * pi / L;
grad_u_an[1] = 0;
grad_u_an[2] = 0;
grad_T_an[0] = -T_x * sin(a_Tx * pi * x / L) * a_Tx * pi / L;
grad_T_an[1] = 0;
grad_T_an[2] = 0;
